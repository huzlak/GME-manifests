apiVersion: networking.gloo.solo.io/v2
kind: RouteTable
metadata:
  name: delegated-329
  namespace: httpbin
  labels:
    expose: "true"
    route-table: middle-329
    passthrough: "true"
spec:
  http:
    - matchers:
      - uri:
          prefix: /test
      forwardTo:
        pathRewrite: /
        autoHostRewrite: true
        destinations:
          - ref:
              name: google
              namespace: httpbin
            kind: EXTERNAL_SERVICE
            port:
              number: 80	  
      #name: sub
      name: pub
