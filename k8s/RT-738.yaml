    apiVersion: networking.gloo.solo.io/v2
    kind: RouteTable
    metadata:
      name: httpbin-738
      namespace: httpbin
      labels:
        expose: "true"
        jwt: "true"
        passthrough: "true"
    spec:
      hosts:
        - httpbin-738.test
      virtualGateways:
        - name: north-south-gw
          namespace: istio-gateways
          cluster: cluster1
      http:
        - name: httpbin-738-renamed
        #- name: httpbin-738
          matchers:
          - uri:
              prefix: /
          delegate:
            routeTables:
              - labels:
                  route-table: middle-738
            sortMethod: ROUTE_SPECIFICITY
          labels:
            route: httpbin-738
