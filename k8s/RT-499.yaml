    apiVersion: networking.gloo.solo.io/v2
    kind: RouteTable
    metadata:
      name: httpbin-499
      namespace: httpbin
      labels:
        expose: "true"
        jwt: "true"
        passthrough: "true"
    spec:
      hosts:
        - httpbin-499.test
      virtualGateways:
        - name: north-south-gw
          namespace: istio-gateways
          cluster: cluster1
      http:
        - name: httpbin-499-renamed
        #- name: httpbin-499
          matchers:
          - uri:
              prefix: /test
          forwardTo:
            pathRewrite: /
            autoHostRewrite: true
            destinations:
              - ref:
                  name: google
                  namespace: httpbin
                kind: EXTERNAL_SERVICE
                port:
                  number: 80
          labels:
            route: httpbin-499
